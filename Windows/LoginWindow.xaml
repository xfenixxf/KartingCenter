<Window x:Class="KartingCenter.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="347" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="10" Margin="-20,10,-20,20">
            <TextBlock Text="Вход в систему" FontSize="20" Foreground="White" 
                       FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>

        <TextBlock Grid.Row="1" Text="Логин (Email):" FontWeight="Bold" Margin="0,5"/>
        <TextBox x:Name="LoginBox" Grid.Row="2" Margin="0,5" Height="35" FontSize="14"
                 KeyDown="LoginBox_KeyDown"/>

        <TextBlock Grid.Row="3" Text="Пароль:" FontWeight="Bold" Margin="0,10,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Margin="0,5" Height="35" FontSize="14"
                     KeyDown="PasswordBox_KeyDown"/>

        <TextBlock x:Name="ErrorText" Grid.Row="5" Foreground="Red" 
                   HorizontalAlignment="Center" Margin="0,10" Visibility="Collapsed"/>

        <Button x:Name="LoginBtn" Grid.Row="6" Content="Войти" 
                Width="120" Height="40" HorizontalAlignment="Center"
                Background="#27AE60" Foreground="White" FontSize="14" FontWeight="Bold"
                Click="LoginBtn_Click"/>
    </Grid>
</Window>